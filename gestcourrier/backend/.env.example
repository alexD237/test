SECRET_KEY=change-me
# False en production.
DEBUG=True
# Hôtes autorisés, séparés par des virgules (requis si DEBUG=False).
ALLOWED_HOSTS=
# Origines CORS autorisées (vide si frontend et API sous la même origine Nginx).
CORS_ALLOWED_ORIGINS=
DB_NAME=gestcourrier
DB_USER=gestcourrier
DB_PASSWORD=change-me
DB_HOST=localhost
DB_PORT=5432
